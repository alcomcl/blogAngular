/* Archivo de configuracion global */
    
    // Creamos una variable que contendrá un json con la url de la Api
    // Esto se hace para importar la url donde sea necesario
    export var Global = {
        url: 'http://localhost:3700/api/'
    };